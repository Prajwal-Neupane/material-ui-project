import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0E2954",
      light: "#1F6E8C",
    },
    secondary: {
      main: "#025464",
      light: "#E8AA42",
    },
    mode: "dark",
  },
});
