import { Box } from "@mui/material";
import React from "react";
import CardComp from "./Card";

const Main = () => {
  return (
    <Box sx={{}} flex={4} p={2}>
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
      <CardComp />
    </Box>
  );
};

export default Main;
