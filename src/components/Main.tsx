import { Box } from "@mui/material";
import React from "react";

const Main = () => {
  return (
    <Box
      sx={{
        backgroundColor: "yellow",
      }}
      flex={4}
      p={2}
    >
      Main
    </Box>
  );
};

export default Main;
